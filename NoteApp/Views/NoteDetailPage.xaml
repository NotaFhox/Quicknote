<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="NoteApp.Views.NoteDetailPage"
             x:DataType="viewModels:NoteDetailViewModel"
             xmlns:viewModels="clr-namespace:NoteApp.ViewModels"
             Title="{Binding Title}"
             BackgroundColor="{StaticResource WindowsBeige}">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Save" 
                     Command="{Binding SaveCommand}"
                     IconImageSource="💾" />
        <ToolbarItem Text="Back" 
                     Command="{Binding BackCommand}"
                     IconImageSource="⬅️" />
    </ContentPage.ToolbarItems>

    <ScrollView Padding="8">
        <StackLayout Spacing="12">
            
            
            <Frame BackgroundColor="{StaticResource ButtonFace}"
                   BorderColor="{StaticResource ControlDark}"
                   CornerRadius="0"
                   HasShadow="False"
                   Padding="12">
                <Label Text="Note Editor"
                       Style="{StaticResource SubHeadline}"
                       HorizontalTextAlignment="Start"
                       Margin="0" />
            </Frame>

            
            <Frame Style="{StaticResource ContentFrame}"
                   Padding="16">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    
                    <StackLayout Grid.Row="0" Spacing="4" Margin="0,0,0,12">
                        <Label Text="Title:"
                               FontAttributes="Bold"
                               TextColor="{StaticResource Black}" />
                        <Frame BackgroundColor="{StaticResource White}"
                               BorderColor="{StaticResource ControlDark}"
                               CornerRadius="0"
                               HasShadow="False"
                               Padding="8">
                            <Entry Text="{Binding Note.Title}"
                                   Placeholder="Enter note title..."
                                   FontSize="16"
                                   BackgroundColor="Transparent" />
                        </Frame>
                    </StackLayout>

                    
                    <StackLayout Grid.Row="1" Spacing="4" Margin="0,0,0,12">
                        <Label Text="Category:"
                               FontAttributes="Bold"
                               TextColor="{StaticResource Black}" />
                        <Frame BackgroundColor="{StaticResource ButtonFace}"
                               BorderColor="{StaticResource ControlDark}"
                               CornerRadius="0"
                               HasShadow="False"
                               Padding="4">
                            <Picker Title="Select Category"
                                    SelectedItem="{Binding Note.Category}"
                                    BackgroundColor="Transparent">
                                <Picker.ItemsSource>
                                    <x:Array Type="{x:Type x:String}">
                                        <x:String>General</x:String>
                                        <x:String>Work</x:String>
                                        <x:String>Personal</x:String>
                                        <x:String>Ideas</x:String>
                                        <x:String>Shopping</x:String>
                                        <x:String>Tasks</x:String>
                                        <x:String>Notes</x:String>
                                        <x:String>Research</x:String>
                                    </x:Array>
                                </Picker.ItemsSource>
                            </Picker>
                        </Frame>
                    </StackLayout>

                    
                    <StackLayout Grid.Row="2" Spacing="4" Margin="0,0,0,12">
                        <Label Text="Tags:"
                               FontAttributes="Bold"
                               TextColor="{StaticResource Black}" />
                        <Frame BackgroundColor="{StaticResource White}"
                               BorderColor="{StaticResource ControlDark}"
                               CornerRadius="0"
                               HasShadow="False"
                               Padding="8">
                            <Entry Text="{Binding Note.Tags}"
                                   Placeholder="Enter tags separated by commas..."
                                   FontSize="14"
                                   BackgroundColor="Transparent" />
                        </Frame>
                    </StackLayout>

                    
                    <Frame Grid.Row="3"
                           BackgroundColor="{StaticResource Secondary}"
                           BorderColor="{StaticResource Primary}"
                           CornerRadius="0"
                           HasShadow="False"
                           Padding="8"
                           Margin="0,0,0,12">
                        <StackLayout Spacing="2">
                            <Label Text="{Binding Note.DateCreated, StringFormat='Created: {0:dd MMM yyyy HH:mm}'}"
                                   Style="{StaticResource MetadataLabel}"
                                   FontSize="12" />
                            <Label Text="{Binding Note.DateModified, StringFormat='Last modified: {0:dd MMM yyyy HH:mm}'}"
                                   Style="{StaticResource MetadataLabel}"
                                   FontSize="12" />
                        </StackLayout>
                    </Frame>

                    
                    <StackLayout Grid.Row="4" Spacing="4" Margin="0,0,0,8">
                        <Label Text="Content:"
                               FontAttributes="Bold"
                               TextColor="{StaticResource Black}" />
                    </StackLayout>

                   
                    <Frame Grid.Row="5"
                           BackgroundColor="{StaticResource White}"
                           BorderColor="{StaticResource ControlDark}"
                           CornerRadius="0"
                           HasShadow="False"
                           Padding="8"
                           VerticalOptions="FillAndExpand">
                        <Editor Text="{Binding Note.Content}"
                                Placeholder="Start typing your note here..."
                                FontSize="14"
                                AutoSize="TextChanges"
                                BackgroundColor="Transparent"
                                VerticalOptions="FillAndExpand"
                                MinimumHeightRequest="200" />
                    </Frame>
                </Grid>
            </Frame>

            
            <Frame BackgroundColor="{StaticResource ButtonFace}"
                   BorderColor="{StaticResource ControlDark}"
                   CornerRadius="0"
                   HasShadow="False"
                   Padding="12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    
                    
                    
                    
                    
                    <Button Grid.Column="1"
                            Text="Save"
                            Style="{StaticResource AccentButton}"
                            Command="{Binding SaveCommand}"
                            WidthRequest="80"
                            Margin="8,0" />
                    
                    <Button Grid.Column="2"
                            Text="Cancel"
                            Command="{Binding BackCommand}"
                            WidthRequest="80" />
                </Grid>
            </Frame>
        </StackLayout>
    </ScrollView>
</ContentPage>